# Copy this file to .env and replace every empty value before starting Docker Compose.
# Generate JWT_SECRET with: openssl rand -hex 32

DB_PASSWORD=
MYSQL_ROOT_PASSWORD=
JWT_SECRET=

# Optional host-side overrides:
# BACKEND_PORT=8080
# FRONTEND_PORT=4200
# JWT_ISSUER=http://localhost:8080
